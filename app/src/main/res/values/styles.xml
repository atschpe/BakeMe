<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="check">
        <item name="colorAccent">@color/colorPrimaryDark</item>
    </style>

    <style name="button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="navigationButtons" parent="button">
        <item name="android:tint">@color/colorAccent</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
    </style>

    <style name="favButton" parent="button">
        <item name="android:button">@drawable/favourite_selector</item>
        <item name="android:contentDescription">@string/favourite_button</item>
    </style>

    <style name="text">
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="labels" parent="text">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">24sp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/half_margin</item>
        <item name="android:background">@drawable/rounded_background</item>
    </style>

</resources>
